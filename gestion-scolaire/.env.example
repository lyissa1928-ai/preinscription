# Backend (gestion-scolaire/backend)
# SQLite : développement sans Docker
DATABASE_URL="file:./dev.db"
# PostgreSQL : avec docker-compose up -d
# DATABASE_URL="postgresql://gestion:gestion_secret@localhost:5432/gestion_scolaire"

PORT=3000
FRONTEND_URL="http://localhost:3001"

# Frontend (gestion-scolaire/frontend) — URL de l’API backend
NEXT_PUBLIC_API_URL="http://localhost:3000"
